---
phase: 3
plan: 04
type: execute
title: Integration Verification and Demo Readiness
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - README.md
  - backend/app/scripts/seed_demo_user.py
  - backend/tests/test_task_execution.py
  - frontend/src/views/task-execution.spec.ts
  - .planning/phases/03-task-execution-log-blocker-loop/03-VALIDATION.md
autonomous: true
requirements_addressed: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08, WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06]
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08, WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06]
must_haves:
  truths:
    - "Phase 3 must create real persisted task, participant, subtask, dependency, work-log, and blocker data."
    - "Phase 3 must not implement full acceptance submission, Owner/project-manager Review, evidence snapshots, notification center, reports, real Git integration, AI review, or board-column customization."
    - "D-01 through D-41 must be visible in either backend behavior, frontend behavior, tests, or documented Phase 4/5 handoff boundaries."
  artifacts:
    - path: "README.md"
      provides: "manual smoke path for Phase 3"
    - path: "backend/tests/test_task_execution.py"
      provides: "backend Phase 3 regression coverage"
    - path: "frontend/src/views/task-execution.spec.ts"
      provides: "frontend Phase 3 regression coverage"
  key_links:
    - from: "03-VALIDATION.md"
      to: "backend/tests/test_task_execution.py"
      via: "Nyquist validation map"
---

# Plan 04 — Integration Verification and Demo Readiness

<objective>
Close Phase 3 by verifying backend and frontend integration, documenting a short demo path, and ensuring every Phase 3 requirement and context decision is covered without leaking Phase 4/5/v2 features into the UI.
</objective>

<user_story>
As a demo reviewer, I want to create a task, record work, mark and resolve a blocker, and open task details in the running app, so that TTCS proves real task execution before acceptance Review is added.
</user_story>

<threat_model>
T-03-13: Documentation or seed data could include credentials or fake production-like secrets. Keep demo data local-only and avoid secret values.
T-03-14: Verification could pass while requirements are dropped. Run backend, frontend, requirement coverage, and decision coverage checks before marking the phase ready.
T-03-15: Demo seed data could fake task state without exercising real APIs/models. Seed only real persisted entities and keep UI rendering API-backed data.
</threat_model>

<must_haves>
- Full backend test suite is green.
- Full frontend typecheck, unit tests, and build are green.
- README includes a Phase 3 smoke path.
- No fake Phase 4/5/v2 capabilities are visible as working features.
- Requirements `TASK-01..08` and `WORK-01..06` are all covered.
</must_haves>

<tasks>

<task id="03-04-01" title="Add demo seed or smoke setup for real Phase 3 entities">
<read_first>
- `backend/app/scripts/seed_demo_user.py`
- `backend/app/services/team_service.py`
- `backend/app/services/project_service.py`
- `backend/app/services/task_service.py`
- `backend/app/services/work_log_service.py`
- `.planning/phases/03-task-execution-log-blocker-loop/03-CONTEXT.md`
</read_first>
<action>
- If useful for local review, extend `backend/app/scripts/seed_demo_user.py` or add a clearly named local-only seed helper to create real team, project, task, participant, subtask, work-log, and blocker rows through services.
- Keep demo seed optional and local-only; do not hard-code production secrets.
- If adding seed data would be too broad, instead document manual API/UI steps in README and leave the seed script unchanged.
</action>
<verify>
<automated>`cd backend && uv run pytest -q tests/test_task_execution.py`</automated>
</verify>
<acceptance_criteria>
- Any Phase 3 demo data is persisted through real models/services, not frontend constants.
- No `.env` secrets or production credentials are committed.
- Seed or smoke path creates at least one task, one work log, and one blocker only if implemented.
- Requirements covered: TASK-01, TASK-05, TASK-06, WORK-01, WORK-04, WORK-06.
</acceptance_criteria>
</task>

<task id="03-04-02" title="Document Phase 3 manual smoke path">
<read_first>
- `README.md`
- `.planning/phases/03-task-execution-log-blocker-loop/03-UI-SPEC.md`
- `.planning/phases/03-task-execution-log-blocker-loop/03-RESEARCH.md`
</read_first>
<action>
- Update `README.md` with a `Phase 3 Task Execution Smoke` section.
- Include exact manual steps: log in, open project board, create task, open drawer, write work log, mark blocker, confirm card shows `阻塞中`, resolve blocker with a note, open `/tasks/:taskId`, and confirm history is visible.
- Keep copy clear that acceptance Review and reports are later phases.
- Preserve existing startup and verification sections.
</action>
<verify>
<automated>`rg -n "Phase 3 Task Execution Smoke|/tasks/:taskId|阻塞中|记录工作日志" README.md`</automated>
</verify>
<acceptance_criteria>
- `README.md` contains heading `Phase 3 Task Execution Smoke`.
- `README.md` mentions `/tasks/:taskId`.
- `README.md` mentions `阻塞中`.
- `README.md` does not describe acceptance approve/reject controls as implemented.
- Requirements covered: TASK-01, TASK-08, WORK-01, WORK-04, WORK-05, WORK-06.
</acceptance_criteria>
</task>

<task id="03-04-03" title="Run full automated verification and close coverage gaps">
<read_first>
- `.planning/phases/03-task-execution-log-blocker-loop/03-VALIDATION.md`
- `backend/tests/test_task_execution.py`
- `frontend/src/views/task-execution.spec.ts`
- `backend/pyproject.toml`
- `frontend/package.json`
</read_first>
<action>
- Run backend targeted tests: `cd backend && uv run pytest -q tests/test_task_execution.py`.
- Run backend full tests: `cd backend && uv run pytest -q`.
- Run frontend typecheck: `cd frontend && npm run typecheck`.
- Run frontend targeted tests: `cd frontend && npm run test:unit -- --run src/views/task-execution.spec.ts`.
- Run frontend full unit/build verification: `cd frontend && npm run test:unit -- --run && npm run build`.
- Fix any failures caused by Phase 3 implementation files only.
- Update `03-VALIDATION.md` task status markers if the execution workflow tracks them during implementation.
</action>
<verify>
<automated>`cd backend && uv run pytest -q && cd ../frontend && npm run typecheck && npm run test:unit -- --run && npm run build`</automated>
</verify>
<acceptance_criteria>
- `cd backend && uv run pytest -q` exits 0.
- `cd frontend && npm run typecheck` exits 0.
- `cd frontend && npm run test:unit -- --run` exits 0.
- `cd frontend && npm run build` exits 0.
- No three consecutive Phase 3 task commits lack automated verification evidence in summaries.
- Requirements covered: TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08, WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06.
</acceptance_criteria>
</task>

<task id="03-04-04" title="Audit phase boundaries before execution summary">
<read_first>
- `.planning/phases/03-task-execution-log-blocker-loop/03-CONTEXT.md`
- `.planning/phases/03-task-execution-log-blocker-loop/03-UI-SPEC.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `frontend/src/views/TaskDetailView.vue`
- `frontend/src/components/task/TaskDrawer.vue`
- `backend/app/api/v1/tasks.py`
</read_first>
<action>
- Search backend and frontend Phase 3 code for premature Phase 4/5/v2 working features.
- Confirm no implemented endpoint path contains `acceptance-submissions`, `acceptance-reviews`, `reports`, `notifications`, `ai-review`, or real Git sync behavior.
- Confirm UI copy does not present acceptance Review, notification center, reports, real Git sync, or AI review as working features.
- Confirm all task data shown on board/detail comes from API/store state, not hard-coded fake cards.
</action>
<verify>
<automated>`rg -n "acceptance-submissions|acceptance-reviews|ai-review|Git sync|通知中心|项目报表" backend/app frontend/src README.md || true`</automated>
</verify>
<acceptance_criteria>
- Any search hits for Phase 4/5/v2 terms are either documentation boundary notes or reserved disabled copy, not working actions.
- `TaskDrawer.vue` does not contain approve/reject Review controls.
- `backend/app/api/v1/tasks.py` does not define acceptance submission or acceptance review endpoints.
- Board task cards are rendered from task API/store data.
- Requirements covered: TASK-01, TASK-02, TASK-03, TASK-08, WORK-01, WORK-04, WORK-05, WORK-06.
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run `cd backend && uv run pytest -q`.
- Run `cd frontend && npm run typecheck && npm run test:unit -- --run && npm run build`.
- Run `rg -n "Phase 3 Task Execution Smoke|/tasks/:taskId|阻塞中|记录工作日志" README.md`.
- Run `rg -n "acceptance-submissions|acceptance-reviews|ai-review|Git sync|通知中心|项目报表" backend/app frontend/src README.md || true` and review any hits.
</verification>

<success_criteria>
- Phase 3 has a documented, real-data demo path.
- Automated backend and frontend verification is green.
- No later-phase features are exposed as implemented.
- All Phase 3 requirements and context decisions are represented in implementation evidence.
</success_criteria>
